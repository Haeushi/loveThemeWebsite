// Simulate a boot process with timing events
document.addEventListener("DOMContentLoaded", function () {
    // Simulate a boot completion
    setTimeout(() => {
         window.location.href = "welcomeScreen/mainPageScreen.html"; // Redirect to mainPage screen
    }, 4000); // welcome screen lasts for 4 seconds
});